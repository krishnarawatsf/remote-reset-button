# remote-reset-automation

Python tools for remote reset and recovery automation with safety and rollback checks.

## One-line
Automates remote device reset/recovery with dry-run and rollback safety mechanisms.

## Quickstart
- Configure `config.yml` and run `python main.py --dry-run`

## Safety
- Always use `--dry-run` before applying live resets
